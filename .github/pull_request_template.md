## What does this change?

<!-- A short summary of what and why. -->

## Checklist

- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] `npm run coros:check` still works (if I touched the COROS client or scoring)
- [ ] No credentials or `.env.local` committed
- [ ] No new COROS *mobile* API calls without an explicit, warned opt-in

## Notes for reviewers

<!-- Anything worth calling out: trade-offs, follow-ups, screenshots. -->
